server:
  port: 8084
application:
   name: ORDER-SERVICE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  data:
    mongodb:
      uri: mongodb://admin:secret123@localhost:27017/orderdb?authSource=admin
      repositories:
        enabled: true

